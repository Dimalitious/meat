# Техническое Задание: Модуль "Раскрой" в форме "Сводка заказов"

**Версия:** 1.1  
**Дата:** 2026-01-23  
**Статус:** Утверждено

---

## 1. Бизнес-контекст

### Проблема
При закупке мясного сырья (например, "Каре баранье неочищенное") необходимо:
1. Преобразовать его в готовые продукты (например, "Каре баранье очищенное", "Перзола", "Кости")
2. Рассчитать себестоимость каждого выходного продукта на основе закупочной цены сырья
3. Распределить выход по заказам клиентов

### Пример
- Закуплено: 100 кг "Каре баранье неочищенное" по 800 ₸/кг = 80,000 ₸
- После раскроя:
  - 40 кг "Каре баранье очищенное" (40% выход)
  - 25 кг "Перзола" (25% выход)
  - 20 кг "Кости" (20% выход)
  - 15 кг "Отходы мясные" (15% — отдельный продукт в справочнике)
- Себестоимость выхода: 80,000 ₸ / 85 кг = 941.18 ₸/кг (отходы не учитываются в расчёте)

---

## 2. Утверждённые бизнес-правила

| Правило | Решение |
|---------|---------|
| **Отходы** | Отдельная позиция из справочника товаров (например, "Отходы мясные") |
| **Частичный раскрой** | ✅ Разрешён. Остаток сырья остаётся доступным для следующего раскроя |
| **Справочник рецептов** | Используются существующие MML (техкарты) |
| **Себестоимость отходов** | Не учитывается — только количественный учёт |
| **Объединение закупок** | ✅ Закупки одного продукта за **один день** объединяются |

---

## 3. Функциональные требования

### 3.1 Новая вкладка "Раскрой" в SvodPage

Добавить вкладку в форму "Сводка заказов":
- `Сводка` (существующая)
- `Закупки` (существующая)  
- **`Раскрой`** (новая)

### 3.2 Структура интерфейса вкладки "Раскрой"

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  [Сводка] [Закупки] [Раскрой]                            Дата: [23.01.2026] │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────────────────┐  ┌─────────────────────────────────────┐  │
│  │ 📦 СЫРЬЁ ИЗ ЗАКУПОК        │  │ 📋 ТРЕБУЕТСЯ ПО ЗАКАЗАМ            │  │
│  ├─────────────────────────────┤  ├─────────────────────────────────────┤  │
│  │ ☐ Каре неочищ.  100 кг 800₸│  │ Каре очищенное      45 кг    🟢    │  │
│  │ ☐ Лопатка неоч.  50 кг 600₸│  │ Перзола             30 кг    🟡    │  │
│  │ ☐ Корейка неоч.  80 кг 750₸│  │ Кости               15 кг    🔴    │  │
│  └─────────────────────────────┘  └─────────────────────────────────────┘  │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────────┐│
│  │ ✂️ РАСКРОЙ                                               [Применить]  ││
│  ├──────────────────┬──────────┬───────────┬───────────┬──────────────────┤│
│  │ Сырьё            │ Исп-но   │ Продукт   │ Выход, кг │ Себестоимость    ││
│  ├──────────────────┼──────────┼───────────┼───────────┼──────────────────┤│
│  │ Каре неочищ.     │ [80]/100 │ Каре очищ.│ [32]      │ 2000.00 ₸/кг     ││
│  │                  │          │ Перзола   │ [20]      │ 2000.00 ₸/кг     ││
│  │                  │          │ Кости     │ [16]      │ 2000.00 ₸/кг     ││
│  │                  │          │ Отходы    │ [12]      │ —                ││
│  │                  │          ├───────────┼───────────┼──────────────────┤│
│  │                  │          │ ИТОГО     │ 80 кг     │ Баланс: ✓        ││
│  │                  │          │ Остаток   │ 20 кг     │ (доступен)       ││
│  └──────────────────┴──────────┴───────────┴───────────┴──────────────────┘│
└─────────────────────────────────────────────────────────────────────────────┘
```

### 3.3 Логика работы

#### Панель "Сырьё из закупок"
- Показывать продукты из закупок на выбранную дату
- **Закупки одного продукта за один день объединяются:**
  - Суммируется количество
  - Рассчитывается средневзвешенная цена
- Checkbox для выбора сырья
- Отображать: Доступно / Всего (например: "80/100 кг")

#### Панель "Требуется по заказам"
- Сумма заказов по продуктам-выходам на эту дату
- Цветовая индикация:
  - 🟢 Зелёный — выход покрывает заказ полностью
  - 🟡 Жёлтый — выход покрывает заказ частично  
  - 🔴 Красный — выход не покрывает заказ
  - ⬜ Серый — нет заказов на этот продукт

#### Таблица раскроя
- Строится на основе **MML (техкарт)** для выбранного сырья
- Колонка "Использовано" — редактируемое поле:
  - Можно указать **меньше** доступного количества (частичный раскрой)
  - Остаток останется для следующих раскроев
- Выходные продукты подгружаются из MML
- **Отходы** — отдельный продукт из справочника (поле `isWaste = true`)
- Поля выхода редактируемые
- Авто-расчёт себестоимости:
  ```
  Себестоимость = (Цена сырья × Использовано) / (Сумма выхода − Отходы)
  ```
- Валидация: `Сумма выхода = Количество использованного сырья`

### 3.4 Действие "Применить раскрой"

При нажатии кнопки:

1. **Создать `ProductionDoc`** со статусом `done` и типом `cutting`
2. **Создать `ProductionInput`** для каждого использованного сырья:
   - `qtyIn` = количество использованного сырья
   - `qtyUsed` = то же значение (полностью использовано в этом раскрое)
   - Связь с `purchaseItemIds`
3. **Создать `ProductionOutput`** для каждого выходного продукта:
   - `qty` = количество выхода
   - `costPrice` = рассчитанная себестоимость (для отходов = 0)
   - `isWaste` = true/false
4. **Создать `ProductionCuttingLine`** для детальной связи input → output
5. **Обновить остаток сырья** — уменьшить доступное количество в закупках

---

## 4. Изменения в базе данных

### 4.1 Существующие модели (используем)

```prisma
model ProductionDoc {
  id            Int       @id @default(autoincrement())
  docNumber     String    @unique
  docDate       DateTime
  status        String    // draft, loaded, cutting, done, canceled
  warehouseId   Int?
  createdById   Int
  createdAt     DateTime  @default(now())
  // ... relations
}

model ProductionInput {
  id              Int       @id @default(autoincrement())
  productionDocId Int
  productId       Int
  purchaseId      Int?
  purchaseItemId  Int?
  qtyIn           Decimal
  qtyUsed         Decimal   @default(0)
  priceIn         Decimal
  // ... relations
}

model ProductionOutput {
  id              Int       @id @default(autoincrement())
  productionDocId Int
  productId       Int
  qty             Decimal
  costPrice       Decimal?
  // ... relations
}
```

### 4.2 Новые поля

```prisma
model ProductionOutput {
  // ... существующие поля ...
  isWaste       Boolean   @default(false)  // Флаг "это отходы"
}

model ProductionDoc {
  // ... существующие поля ...
  docType       String    @default("production")  // production, cutting
}
```

---

## 5. API Endpoints

### 5.1 Получение доступного сырья

```
GET /api/cutting/available-raw-materials?date=2026-01-23
```

**Response:**
```json
[
  {
    "productId": 123,
    "productName": "Каре баранье неочищенное",
    "totalQty": 100,
    "usedQty": 20,
    "availableQty": 80,
    "avgPrice": 800.00,
    "purchaseItemIds": [45, 46, 47]
  }
]
```

### 5.2 Получение MML для сырья

```
GET /api/cutting/mml-for-product/:productId
```

**Response:**
```json
{
  "mmlId": 5,
  "mmlName": "Разделка каре баранье",
  "inputProductId": 123,
  "outputs": [
    { "productId": 124, "productName": "Каре очищенное", "yieldPercent": 40, "isWaste": false },
    { "productId": 125, "productName": "Перзола", "yieldPercent": 25, "isWaste": false },
    { "productId": 126, "productName": "Кости", "yieldPercent": 20, "isWaste": false },
    { "productId": 999, "productName": "Отходы мясные", "yieldPercent": 15, "isWaste": true }
  ]
}
```

### 5.3 Применение раскроя

```
POST /api/cutting/apply
```

**Request:**
```json
{
  "date": "2026-01-23",
  "warehouseId": 1,
  "items": [
    {
      "inputProductId": 123,
      "inputQty": 80,
      "purchaseItemIds": [45, 46],
      "mmlId": 5,
      "outputs": [
        { "productId": 124, "qty": 32, "isWaste": false },
        { "productId": 125, "qty": 20, "isWaste": false },
        { "productId": 126, "qty": 16, "isWaste": false },
        { "productId": 999, "qty": 12, "isWaste": true }
      ]
    }
  ]
}
```

**Response:**
```json
{
  "success": true,
  "productionDocId": 42,
  "docNumber": "CUT-2026-0001"
}
```

---

## 6. UI Компоненты

| Компонент | Описание |
|-----------|----------|
| `CuttingTab` | Основной компонент вкладки "Раскрой" |
| `RawMaterialsPanel` | Левая панель — сырьё из закупок с чекбоксами |
| `OrderRequirementsPanel` | Правая панель — требования по заказам |
| `CuttingTable` | Таблица раскроя с редактируемыми полями |
| `CuttingRowGroup` | Группа строк для одного сырья (input + outputs) |

---

## 7. Валидация

| Правило | Сообщение об ошибке |
|---------|---------------------|
| Сумма выхода ≠ Использованному сырью | "Сумма выхода (85 кг) не равна использованному сырью (80 кг)" |
| Использовано > Доступно | "Нельзя использовать больше доступного количества" |
| Нет выходов > 0 | "Укажите хотя бы один выход > 0" |
| Не выбрано сырьё | "Выберите сырьё для раскроя" |

---

## 8. План реализации

### Этап 1: База данных (0.5 часа)
- [ ] Добавить поле `isWaste` в `ProductionOutput`
- [ ] Добавить поле `docType` в `ProductionDoc`
- [ ] Выполнить миграцию

### Этап 2: Backend API (2 часа)
- [ ] `GET /api/cutting/available-raw-materials` — с группировкой по дню
- [ ] `GET /api/cutting/mml-for-product/:productId`
- [ ] `POST /api/cutting/apply`

### Этап 3: Frontend UI (3 часа)
- [ ] Компонент `CuttingTab`
- [ ] Панели сырья и заказов
- [ ] Таблица раскроя с редактированием
- [ ] Расчёт себестоимости
- [ ] Валидация

### Этап 4: Интеграция (1 час)
- [ ] Добавить вкладку в `SvodPage`
- [ ] Тестирование end-to-end

---

**Общая оценка:** ~6.5 часов

**Готов к реализации! ✅**
