# Railway PostgreSQL Connection (Public)
DATABASE_URL="postgresql://postgres:bDdzZCoCnbRJMxEZaEbqhRkyhgVnRpHn@metro.proxy.rlwy.net:20662/railway"

# JWT Secret for authentication
JWT_SECRET="meat-erp-jwt-secret-key-2024"

# Server Port
PORT=3000

# RBAC: Allow old JWT tokens without authVersion field (set false after all users re-login)
JWT_GRACE_OLD=true

# RBAC: Fallback to legacy User.role if no UserRole records (set false after full migration)
RBAC_FALLBACK=true

# RBAC: Admin seed credentials (used by `npm run seed:dev` / ensureRbacSeededRuntime)
ADMIN_SEED_USERNAME=admin
ADMIN_SEED_PASSWORD=Meat!Erp#2026$Secure

# RBAC: Cache TTL (ms) — lower = faster revocation, higher = less DB load
USER_META_TTL_MS=15000
PERMS_TTL_MS=120000
