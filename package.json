{
    "name": "z99-erp-monorepo",
    "private": true,
    "engines": {
        "node": "22.x"
    },
    "scripts": {
        "build": "cd server && npm install && npm run build",
        "start": "cd server && npx prisma migrate deploy && npm start"
    }
}